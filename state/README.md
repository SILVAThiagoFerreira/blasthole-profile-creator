# State

Arquivos de preferência persistida do app.

- `user_preferences.json`: seed da última configuração válida usada na geração
- Não armazena imagem da malha
- É lido pelo site estático e sobrescrito quando o usuário salva memória no navegador
