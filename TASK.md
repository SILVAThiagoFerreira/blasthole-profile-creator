# TASK

Estruturar o projeto como um sistema configurável, validado e rastreável para geração de lâminas técnicas OpenBlast.

Critério atual: criar documentação obrigatória, configurar `config.json`, organizar `src/`, manter um único ponto de entrada e validar a execução final.

Critério de ajuste atual: manter a terminologia de cartuchos/pré-cortes consistente entre `config.json`, o seed e a lâmina renderizada.

Critério complementar: aceitar upload opcional de logo e imagem da malha na geração online, com rejeição explícita de arquivos inválidos.
