# CHECKLIST

- `config.json` existe e é lido pela aplicação.
- `app.py` permanece como entrada única.
- `src/` contém leitura, validação e pipeline.
- `input/`, `output/` e `logs/` existem.
- Nenhum arquivo crítico está vazio.
- A geração ocorre apenas após validação.
- O manifesto é escrito em `logs/`.
- Os testes passam.
